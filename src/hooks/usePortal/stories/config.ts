import { StoryConfig } from "storybook_utils/packageConstants.ts"

export default {
  description: `Simple hook to construct a function that renders any passed in content inside of a portal, with DOM updates handled automatically. Creation of elements or selecting of existing elements can be controlled through options.`,
  alternative: "Portal",
} as StoryConfig
